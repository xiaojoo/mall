spring.application.name=mall-search
spring.cloud.nacos.config.server-addr=${SERVER_IP:localhost}:8848
spring.cloud.nacos.username=${NACOS_USERNAME:nacos}
spring.cloud.nacos.password=${NACOS_PASSWORD:nacos}
